# Home Assistant Apprise Notify

Home Assistant custom notify integration for sending notifications through an Apprise API server.

This repository is intentionally built in small PRs. The first implementation target is a HACS-compatible custom integration that registers a Home Assistant notify service backed by Apprise API.
